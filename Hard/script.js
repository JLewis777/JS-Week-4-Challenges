// Create 2 buttons
// One that when clicked, turns the background color red (Event listeners)
// The other when clicke, turns the background white
// Define each button tag as class styles (Names already in CSS)
// When buttons are clicked, the class names should be changed accordingly
// Create a single function that takes an input of a class name, then adds it to the classList with an exclusive class name

// function btnClick(btn) {
//   if (btn == "bg-red") {
//     document.querySelector("body").classList.add("bg-red");
//     document.querySelector("body").classList.remove("bg-white");
//   } else {
//     document.querySelector("body").classList.add("bg-white");
//     document.querySelector("body").classList.remove("bg-red");
//   }
//   console.log(document.querySelector("body").classList);
//   console.log(document.querySelector("body").classList);
// }
